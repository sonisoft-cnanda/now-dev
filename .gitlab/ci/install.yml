install:
  stage: install
  script:
    - npm ci --prefer-offline
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - .npm/
    policy: pull-push
  artifacts:
    paths:
      - node_modules/
    expire_in: 1 hour
